public class StaleLeadProcessingScheduler implements Schedulable {

    public void execute(SchedulableContext sc) {
        StaleLeadProcessingBatch batch = new StaleLeadProcessingBatch(200);
        Database.executeBatch(batch, 200);
    }
}
